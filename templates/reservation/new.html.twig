<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Travelia Admin — Nouvelle réservation</title>
    <link rel="stylesheet" href="{{ asset('css/admin.css') }}">

    <style>
        .content-card {
            background: var(--panel);
            border: 1px solid var(--gbord);
            border-radius: 24px;
            padding: 2rem;
            backdrop-filter: blur(20px);
            margin-top: 1.5rem;
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.8rem;
            gap: 1rem;
        }

        .section-header h2 {
            font-family: var(--serif);
            font-size: 2.1rem;
            font-weight: 300;
        }

        .back-btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.85rem 1.2rem;
            border-radius: 12px;
            text-decoration: none;
            font-size: 0.72rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 0.08em;
            background: rgba(255,255,255,0.04);
            border: 1px solid rgba(255,255,255,0.08);
            color: var(--gold2);
        }

        .back-btn:hover {
            background: rgba(255,255,255,0.08);
        }
    </style>
</head>
<body>

    <aside class="app-sidebar">
        <div class="logo-area">
            Travel<em>ia</em>
        </div>

        <nav class="nav-menu">
            <a href="{{ path('admin_dashboard') }}" class="nav-item">
                <span class="nav-icon">⬚</span>
                Tableau de bord
            </a>

            <a href="{{ path('app_hebergement_index') }}" class="nav-item">
                <span class="nav-icon">🏨</span>
                Hébergements
            </a>

            <a href="{{ path('app_reservation_index') }}" class="nav-item active">
                <span class="nav-icon">📘</span>
                Réservations
            </a>

            <a href="#" class="nav-item">
                <span class="nav-icon">🛶</span>
                Activités
            </a>

            <a href="{{ path('app_admin_travelers') }}" class="nav-item">
                <span class="nav-icon">👥</span>
                Clients
            </a>

            <a href="#" class="nav-item">
                <span class="nav-icon">⚙</span>
                Paramètres
            </a>
        </nav>

        <div class="sidebar-user">
            <div class="user-avatar"></div>
            <div class="user-info">
                <span>Admin</span>
                <small>Accès complet</small>
            </div>
        </div>
    </aside>

    <main class="app-main">
        <div class="glow-bg"></div>

        <header class="header">
            <div class="header-top">
                <div class="header-greeting">
                    <h1>Créer une <em>réservation</em></h1>
                    <p style="font-size: 0.9rem; opacity: 0.5; margin-top: 0.5rem;">
                        Ajoute une nouvelle réservation à la plateforme Travelia.
                    </p>
                </div>
                <div class="date-pill">Nouvelle entrée</div>
            </div>
        </header>

        <section class="content-card">
            <div class="section-header">
                <h2>Formulaire de réservation</h2>
                <a href="{{ path('app_reservation_index') }}" class="back-btn">← Retour à la liste</a>
            </div>

            {{ include('reservation/_form.html.twig', {
                'button_label': 'Enregistrer',
                'form_action': path('app_reservation_new')
            }) }}
        </section>
    </main>

    <script src="{{ asset('js/admin.js') }}"></script>
</body>
</html>
